{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "dist": "build"
        }
      },
      {
        "src": "backend/jobs.json",
        "destination":"http://localhost:5000/"
      }
    ],
    "routes": [
      {
        "src": "/jobs",
        "destination": "http://localhost:5000/jobs"
      },
      {
        "src": "/:path*",
        "destination": "http://localhost:5000/:path*"
      }
    ],
    "rewrites": [
      {
        "source": "/:path*",
        "destination": "http://localhost:5000/:path*"
      }
    ],
    "devCommand": {
      "frontend": "npm run dev",
      "backend": "npm run server"
    }
  }
  